# Simple comment

=begin
Multiline
comment
=end

# required "something" - loads a ruby module in a file

=begin
There's something called Kernel. I think it's kind of the "window" object in JS, or "the global scope".

When we call [puts "Something"] it's equivalent to say [Kernel.puts("Something")].
Also true for the "open" method.
=end
